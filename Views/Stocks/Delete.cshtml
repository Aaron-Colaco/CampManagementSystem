@model WebApplication4.Models.Stock

@{
    ViewData["Title"] = "Delete";
}

<h1>Delete</h1>

<h3>Are you sure you want to delete this?</h3>

<div>
    
    <hr />
    <dl class="row">
        <dt class="col-sm-2">
            @Html.DisplayNameFor(model => model.StockNumber)
        </dt>
        <dd class="col-sm-10">
            @Html.DisplayFor(model => model.StockNumber)
        </dd>

        <dt class="col-sm-2">
            Item
        </dt>
        <dd class="col-sm-10">
            @Html.DisplayFor(model => model.Items.Name)
        </dd>
    </dl>

    <input type="text" value="@ViewBag.Name" readonly class="form-control mb-3" />

    <form asp-action="Delete">
        <input asp-for="ItemId" type="hidden" value="@ViewBag.ItemId" />
        <input type="hidden" name="Page" value="@ViewBag.Page" />
        <input type="hidden" name="SearchTerm" value="@ViewBag.SearchTerm" />
        <input type="hidden" asp-for="StockId" />

        <input type="submit" value="Delete" class="btn btn-danger" /> 
        <a asp-action="Index" asp-route-Page="@ViewBag.Page" asp-route-SearchTerm="@ViewBag.SearchTerm">Back to List</a>

    </form>
</div>
